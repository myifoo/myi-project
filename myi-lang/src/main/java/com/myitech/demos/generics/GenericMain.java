package com.myitech.demos.generics;

/**
 * Description:
 *      1. Allows a type or method to operate on objects of various types while
 *      providing compile-time type safety. Java 是静态编程语言，在编译阶段有很严格的类型检测规范，
 *      跟C++很像，这样的话，要实现泛型就需要语言级别的特性来支持。而像python，ruby这样的语言，泛型
 *      是与生俱来的。
 *
 *      2. Generic type 与 C++ 模板类有本质的不同， There aren't multiple copies of the code--not in source,
 *      not in binary, not on disk and not in memory. If you are a C++ programmer, you'll understand that
 *      this is very different than a C++ template. 要知道，每一个C++模板类会在编译时，由编译器生成对应类型的
 *      实体类，在程序运行时，其实是存在多个类的。
 *
 * Created by A.T on 2018/01/31
 */
public class GenericMain {

    public static void main(String[] args) {
//        GenericSample.castType();
//        GenericSample.showWildCards();

    }
}
